---
- name: Gathering Job Responses
  hosts: all
  vars:
    ansible_python_interpreter: /QOpenSys/pkgs/bin/python3
    JOB_NAME: PRUEBA
  collections:
    - ibm.power_ibmi
  connection: ssh
  gather_facts: true
  tasks:
    
    - name: check if
      ibmi_cl_command:
        cmd: system "STRSBS SBSD(M400LIB/M400SBS)"
        
